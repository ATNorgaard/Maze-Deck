{
  "name": "@maze-deck/session-worker",
  "version": "0.1.0",
  "description": "One Durable Object per session: the authority for a live crossing.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build:app": "npm --prefix ../../apps/table run build",
    "deploy": "npm run build:app && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.86.0"
  }
}
