{
  "name": "maze-deck",
  "version": "0.1.0",
  "description": "The repo root. Exists for Vercel and for nothing else — see .design-sync/NOTES.md before adding anything here.",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "install:all": "cd packages/ui && npm ci && cd ../../apps/table && npm ci",
    "build": "cd apps/table && npm run build",
    "dev": "vercel dev"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.6.3"
  }
}
