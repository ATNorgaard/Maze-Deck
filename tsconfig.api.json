{
  "//": "Type-checking the authority functions, and NOTHING ELSE. Deliberately not named tsconfig.json: Vercel reads a root tsconfig.json to compile api/, and this one is wrong for that job -- noEmit produces an empty function, and module:ESNext emits import syntax into a file the CommonJS root makes Node parse as script. Every function 500d until this was renamed. Vercel now generates its own.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023", "DOM"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "types": ["node"],

    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,

    "esModuleInterop": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "isolatedModules": true,
    "noEmit": true
  },
  "include": ["api/**/*.ts"]
}
