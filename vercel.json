{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "installCommand": "npm install --no-audit --no-fund && cd packages/ui && npm ci && cd ../../apps/table && npm ci",
  "buildCommand": "cd apps/table && npm run build",
  "outputDirectory": "apps/table/dist",

  "functions": {
    "api/**/*.ts": {
      "maxDuration": 15
    }
  }
}
